import { useStyles } from "./styles";
import { Container, Divider, Flex, Stack, Text } from "@mantine/core";

const ThirdSlide = () => {
  const { classes } = useStyles();
  return (
    <div >
     
    </div>
  );
};

export default ThirdSlide;
